name: Test Arbigram

# Deliberately not part of the build. The fork's tests cover two modules that
# depend on nothing, so they cost a fraction of a build — but a test target that
# fails to configure would otherwise cost a whole IPA. Kept apart, a broken test
# run is a cheap run to fix rather than a lost release.
on:
  workflow_dispatch:
  push:
    branches:
      - arbigram
    paths:
      - 'submodules/ArbigramSettings/**'
      - 'submodules/ArbigramCore/**'
      - '.github/workflows/arbigram-tests.yml'

concurrency:
  group: arbigram-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: macos-26
    timeout-minutes: 60

    steps:
      - name: Check out
        uses: actions/checkout@v5
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: Verify the upstream patch set
        run: python3 arbigram/verify-patches.py

      - name: Select Xcode
        run: |
          XCODE_VERSION=$(python3 -c "import json;print(json.load(open('versions.json'))['xcode'])")
          sudo xcode-select -s "/Applications/Xcode_$XCODE_VERSION.app/Contents/Developer"
          xcodebuild -version

      # The same absolute path the build uses, so both share one bazel cache.
      - name: Stage source at a stable path
        run: |
          sudo mkdir -p /Users/Shared
          sudo chown -R "$(whoami)" /Users/Shared
          cp -R "$GITHUB_WORKSPACE" /Users/Shared/
          mv "/Users/Shared/$(basename "$GITHUB_WORKSPACE")" /Users/Shared/arbigram

      - name: Restore bazel cache
        uses: actions/cache/restore@v4
        with:
          path: ~/arbigram-bazel-cache
          key: bazel-${{ runner.os }}-${{ hashFiles('versions.json') }}-${{ github.sha }}
          restore-keys: |
            bazel-${{ runner.os }}-${{ hashFiles('versions.json') }}-
            bazel-${{ runner.os }}-

      # Make.py insists on a configuration even for tests, and the values do not
      # reach a test that never builds the app. Real ones stay in the build.
      - name: Write build configuration
        env:
          BUNDLE_ID: ${{ secrets.ARBIGRAM_BUNDLE_ID }}
          TEAM_ID: ${{ secrets.ARBIGRAM_TEAM_ID }}
        run: |
          cd /Users/Shared/arbigram
          python3 - <<'PY'
          import json, os
          config = {
              'bundle_id': os.environ['BUNDLE_ID'],
              'api_id': '1',
              'api_hash': '0123456789abcdef0123456789abcdef',
              'team_id': os.environ['TEAM_ID'],
              'app_center_id': '0',
              'is_internal_build': 'true',
              'is_appstore_build': 'false',
              'appstore_id': '0',
              'app_specific_url_scheme': 'tg',
              'premium_iap_product_id': '',
              'enable_siri': False,
              'enable_icloud': False,
          }
          with open('build-system/arbigram-configuration.json', 'w') as f:
              json.dump(config, f, indent=4)
          PY

      - name: Install signing material
        env:
          P12_BASE64: ${{ secrets.ARBIGRAM_P12_BASE64 }}
          PROFILE_BASE64: ${{ secrets.ARBIGRAM_PROFILE_BASE64 }}
        run: |
          cd /Users/Shared/arbigram
          SIGN_DIR=build-system/arbigram-codesigning
          mkdir -p "$SIGN_DIR/certs" "$SIGN_DIR/profiles"
          echo "$P12_BASE64" | base64 --decode > "$SIGN_DIR/certs/Development.p12"
          echo "$PROFILE_BASE64" | base64 --decode > "$SIGN_DIR/profiles/Telegram.mobileprovision"

      # --target, never the default suite: Tests/AllTests still names a call
      # test target that does not exist, so the default would fail to configure
      # before reaching anything of the fork's.
      - name: Run the Arbigram tests
        env:
          CODESIGNING_P12_PASSWORD: ${{ secrets.ARBIGRAM_P12_PASSWORD }}
        run: |
          cd /Users/Shared/arbigram
          xcrun simctl list devices available | head -40

          python3 build-system/Make/ImportCertificates.py \
            --path build-system/arbigram-codesigning/certs

          python3 -u build-system/Make/Make.py \
            --cacheDir="$HOME/arbigram-bazel-cache" \
            --bazelUserRoot=/private/var/tmp/_bazel_arbigram \
            test \
            --configurationPath=build-system/arbigram-configuration.json \
            --codesigningInformationPath=build-system/arbigram-codesigning \
            --target //submodules/ArbigramSettings:ArbigramSettingsTests

      - name: Save bazel cache
        if: always()
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ~/arbigram-bazel-cache
          key: bazel-${{ runner.os }}-${{ hashFiles('versions.json') }}-${{ github.sha }}
